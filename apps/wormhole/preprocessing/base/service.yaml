apiVersion: v1
kind: Service
metadata:
  name: preprocessing
spec:
  selector:
    app: preprocessing
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5002
  type: ClusterIP